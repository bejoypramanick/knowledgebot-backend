# Dockerfile.pdf-processor-layered
# PDF Processing: Uses PDF processor layer
# Target size: ~255MB (ML + PDF processing + handler)

FROM knowledgebot-pdf-processor-layer:latest

# Copy only the handler
COPY microservices/pdf-processor-handler.py ${LAMBDA_TASK_ROOT}/lambda_function.py

CMD ["lambda_function.lambda_handler"]
