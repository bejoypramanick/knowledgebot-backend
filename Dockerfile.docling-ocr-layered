# Dockerfile.docling-ocr-layered
# Docling OCR Processing: Uses optimized OCR layer
# Target size: ~855MB (OCR layer + handler)
# Much smaller than docling-full

FROM knowledgebot-docling-ocr-layer:latest

# Copy only the handler
COPY microservices/docling-ocr-handler.py ${LAMBDA_TASK_ROOT}/lambda_function.py

CMD ["lambda_function.lambda_handler"]
