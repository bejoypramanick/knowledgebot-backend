# DynamoDB MCP Server Dockerfile
# Based on the official AWS ECR image

FROM public.ecr.aws/awslabs-mcp/awslabs/dynamodb-mcp-server:latest

# Set environment variables (the base image handles the rest)
ENV AWS_REGION=${AWS_REGION:-us-east-1}

# The official image already has the correct CMD and health check
