# Requirements for Docker-based Lambda functions
# These contain ONLY heavy library installations

# Docling Library Handler
docling>=1.0.0
pypdfium2>=4.0.0

# Pinecone Library Handler  
pinecone-client>=3.0.0

# Neo4j Library Handler - REMOVED
# Neo4j operations now handled by Neo4j MCP Server

# OpenAI Library Handler
openai>=1.0.0

# OpenAI Agents SDK
openai-agents>=0.1.0

# Sentence Transformer Library Handler - REMOVED
# Embeddings are now handled directly by Pinecone MCP Server

# Common dependencies
boto3>=1.26.0
botocore>=1.29.0
