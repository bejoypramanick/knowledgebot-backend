# Dockerfile.chat-generator-layered
# ML: Uses ML layer (includes OpenAI + agents)
# Target size: ~405MB (ML + handler)

FROM knowledgebot-ml-layer:latest

# Copy only the handler
COPY microservices/chat-generator-handler.py ${LAMBDA_TASK_ROOT}/lambda_function.py

CMD ["lambda_function.lambda_handler"]
