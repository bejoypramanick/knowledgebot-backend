# Dockerfile.rag-search-layered
# ML: Uses ML layer (includes all ML dependencies)
# Target size: ~405MB (ML + handler)

FROM knowledgebot-ml-layer:latest

# Copy only the handler
COPY microservices/rag-search-handler.py ${LAMBDA_TASK_ROOT}/lambda_function.py

CMD ["lambda_function.lambda_handler"]
