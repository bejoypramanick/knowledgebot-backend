# Requirements for Sentence Transformer Library Handler
# Simplified for Lambda Docker environment

# Use PyTorch CPU version from PyTorch index
torch==2.0.1+cpu

# Core scientific libraries
numpy>=1.24.0,<1.25.0
scipy>=1.10.0,<1.11.0

# Use older versions with better wheel support
transformers==4.20.1
sentence-transformers==2.2.2

# Note: tokenizers and sentencepiece are installed separately in Dockerfile

# Common dependencies
boto3>=1.26.0
botocore>=1.29.0
