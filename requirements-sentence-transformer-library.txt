# Requirements for Sentence Transformer Library Handler
# Optimized for Lambda Docker environment

# Pre-compiled PyTorch and transformers (CPU-only)
torch==2.0.1+cpu
transformers==4.30.2
sentence-transformers==2.2.2

# Pre-compiled scientific libraries (avoid compilation)
numpy==1.24.3
scipy==1.10.1
scikit-learn==1.3.0

# Common dependencies
boto3>=1.26.0
botocore>=1.29.0
