# Requirements for Sentence Transformer Library Handler
# Using newer versions with prebuilt wheels (no Rust compilation needed)

# Use PyTorch CPU version from PyTorch index
torch==2.0.1+cpu

# Core scientific libraries
numpy>=1.24.0,<1.25.0
scipy>=1.10.0,<1.11.0

# Use newer versions with better prebuilt wheel support
transformers>=4.30.0,<4.35.0
sentence-transformers>=2.2.2,<2.3.0

# Tokenizers with prebuilt wheels (≥ 0.13.3 has manylinux wheels)
tokenizers>=0.13.3,<0.15.0

# SentencePiece with prebuilt wheels
sentencepiece>=0.1.99,<0.2.0

# Common dependencies
boto3>=1.26.0
botocore>=1.29.0
