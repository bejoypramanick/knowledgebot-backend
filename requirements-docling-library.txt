# Requirements for Docling Library Handler
# Optimized for Lambda Docker environment

# Core Docling dependencies
docling>=1.0.0
pypdfium2>=4.0.0

# Pre-compiled scientific libraries (avoid compilation)
numpy==1.24.3
scipy==1.10.1
scikit-learn==1.3.0

# Common dependencies
boto3>=1.26.0
botocore>=1.29.0
