# Dockerfile.table-detector-layered
# Table Detection: Uses table detector layer
# Target size: ~455MB (ML + table detection + handler)

FROM knowledgebot-table-detector-layer:latest

# Copy only the handler
COPY microservices/table-detector-handler.py ${LAMBDA_TASK_ROOT}/lambda_function.py

CMD ["lambda_function.lambda_handler"]
