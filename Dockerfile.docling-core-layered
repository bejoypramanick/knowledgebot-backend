# Dockerfile.docling-core-layered
# Docling Core Processing: Uses Docling core layer
# Target size: ~555MB (ML + Docling core + handler)

FROM knowledgebot-docling-core-layer:latest

# Copy only the handler
COPY microservices/docling-ocr-handler.py ${LAMBDA_TASK_ROOT}/lambda_function.py

CMD ["lambda_function.lambda_handler"]
