# Dockerfile.docling
# Document processing runtime: Python 3.12 Lambda base with Docling dependencies
FROM public.ecr.aws/lambda/python:3.12

LABEL maintainer="KnowledgeBot Team"
ENV LANG=C.UTF-8
ENV LC_ALL=C.UTF-8
ENV PIP_DISABLE_PIP_VERSION_CHECK=1
ENV PIP_NO_CACHE_DIR=1

# Skip system dependencies - try with pre-compiled Python packages first
# Docling packages may have ARM64 wheels available

# Upgrade pip and install core dependencies
RUN python -m pip install --upgrade pip setuptools wheel

# Install core Python packages
RUN python -m pip install --no-cache-dir \
    "boto3>=1.34.0" \
    "botocore>=1.34.0" \
    "openai>=1.12.0" \
    "openai-agents>=0.1.0" \
    "pinecone-client>=3.1.0" \
    "neo4j>=5.24.0" \
    "httpx>=0.25.0" \
    "orjson>=3.9.0" \
    "structlog>=23.2.0" \
    "pydantic>=2.5.0" \
    "typing-extensions>=4.9.0"

# Install Docling with CPU-only PyTorch for Lambda environment
# Using the extra-index-url method as recommended in Docling docs for CPU-only
RUN python -m pip install --no-cache-dir \
    --extra-index-url https://download.pytorch.org/whl/cpu \
    "torch>=2.1.0" \
    && python -m pip install --no-cache-dir \
    "docling>=2.25.0"

# Copy application code
COPY agent-toolkit/ ${LAMBDA_TASK_ROOT}/

# Ensure proper Python module structure
RUN ls -la ${LAMBDA_TASK_ROOT}/ && \
    python -c "import sys; print('Python path:', sys.path)" && \
    python -c "import os; print('Files in task root:', os.listdir('${LAMBDA_TASK_ROOT}'))" && \
    python -c "import lambda_handlers; print('lambda_handlers imported successfully')"

# Set the Lambda handler function
# AWS Lambda will call the specified handler function when invoked
CMD ["lambda_handlers.lambda_handler_knowledge_document_ingestion"]
