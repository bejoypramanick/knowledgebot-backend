# ==============================
# KnowledgeBot Backend Requirements
# Compatible with AWS Lambda Python 3.12 Runtime
# ==============================

# Core OpenAI and Agent Framework
openai==1.13.3
openai-agents>=0.3.3
pydantic==2.8.2

# AWS SDK for Lambda environment
boto3>=1.34.0
botocore>=1.34.0

# Vector Database
pinecone>=3.1.0

# Graph Database
neo4j>=5.24.0

# Document Processing (Docling - advanced PDF processing, OCR, table detection)
# Self-contained: includes pypdfium2 (PDF), EasyOCR (OCR), and ML models (table detection)
# Installation: pip install docling --extra-index-url https://download.pytorch.org/whl/cpu
# Features used: PyPdfium backend, OCR for scanned PDFs, table structure detection, hierarchical chunking
docling>=2.25.0

# Machine Learning Stack (Python 3.12 compatible)
torch>=2.1.0
torchvision>=0.16.0  # Required for transformers AutoProcessor
transformers>=4.36.0
sentence-transformers>=2.2.2
numpy>=1.26.0,<2.0.0  # NumPy 2.x may have compatibility issues
tensorflow>=2.14.0

# Data Processing
pandas>=2.2.0

# HTTP and API clients
httpx>=0.25.0
requests>=2.31.0

# Performance and JSON processing
orjson>=3.9.0

# Logging and monitoring
structlog>=23.2.0

# Type hints and validation
pydantic>=2.12.0
typing-extensions>=4.9.0

# Standard library extensions (usually included in Python 3.12)
# json, os, logging, asyncio, datetime, hashlib, pathlib are built-in
