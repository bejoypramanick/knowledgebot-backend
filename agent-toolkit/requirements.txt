# ==============================
# Core Dependencies (Others pulled transitively)
# ==============================

# OpenAI and Agents
openai==1.12.0
openai-agents>=0.1.0            # Re-enabled the agents SDK (check version compatibility)

# AWS SDK
boto3>=1.35.0

# Databases
pinecone-client>=3.1.0
neo4j>=5.24.0

# Document Processing
docling>=2.25.0                 # Use a more recent docling version with improved Python support

# ML Stack (Pinned to avoid build issues)
torch==2.1.0
transformers==4.36.0
sentence-transformers==2.2.2
numpy==1.26.4
tensorflow==2.20.0

# Data Processing
pandas>=2.2.2

# Explicit pins (may be needed for compatibility)
httpx==0.25.0        # OpenAI client dependency
orjson==3.9.10       # Performance optimization
structlog==23.2.0    # Logging
